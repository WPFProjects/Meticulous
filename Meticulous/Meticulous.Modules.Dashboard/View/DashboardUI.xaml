<UserControl x:Class="Meticulous.Modules.Dashboard.View.DashboardUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Meticulous.Modules.Dashboard.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Dashboard" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center" >
            <TextBlock Text="Last Updated: " 
                       FontSize="16" />
            <TextBlock Text="{Binding LastUpdated}" 
                       FontSize="16" />

            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <local:DashboardMetricCard x:Name="TotalServices"/>
            <local:DashboardMetricCard x:Name="LocalServices"/>
            <local:DashboardMetricCard x:Name="Critical"/>
            <local:DashboardMetricCard x:Name="CPUUsage"/>
        </StackPanel>
        
        <local:SystemPerformaceChartView Grid.Row="2"
                                         Grid.Column="0"/>
        
        <local:RecentAlerts Grid.Row="2"
                            Grid.Column="1"/>

        <local:ActiveServices Grid.Row="3"/>

    </Grid>
</UserControl>
